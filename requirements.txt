# AnchorFamily core + AnchorFamily benchmark-suite dependencies
# Python 3.11 is recommended by the upstream benchmark; Python 3.12 also works for the CPU anchor tools.

# Numeric/data stack
numpy>=1.26.4,<3
pandas==2.3.1
pyarrow>=16.0.0
scipy>=1.13.0
scikit-learn==1.7.1

# PyTorch stack used by upstream benchmark data loaders and MIMIC/HumanActivity tensors
torch==2.6.0
torchvision==0.21.0

# vendor/upstream_benchmark/runtime utilities
tqdm==4.67.1
requests>=2.32.0
PyYAML>=6.0.1
coloredlogs==15.0.1
accelerate==1.10.0
einops==0.8.1
ptflops==0.7.4
wandb==0.21.1

# Local package install is still needed after this file:
#   pip install -e .
